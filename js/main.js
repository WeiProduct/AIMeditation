// ===== i18n =====
const I18N = {
  'zh-CN': {
    skip: '跳到主要内容',
    brandName: 'AI冥想',
    navHow: '使用方式', navFeatures: '功能特色', navScreenshots: '应用截图', navFaq: '常见问题', navGet: '获取 App',
    heroEyebrow: '✦ 你的 AI 冥想教练',
    heroTitle: '睡得更深，想得更少，活得更专注',
    heroSubtitle: 'AI 冥想教练陪你呼吸、入睡、平复焦虑——26 段精选音频，全程无需注册。',
    downloadBtn: 'App Store 下载', learnMore: '了解更多',
    trust1: '无需注册', trust2: '数据本地存储', trust3: '中英双语',
    breatheIn: '吸气', breatheOut: '呼气',
    chipA: '7 天深睡计划', chipB: '26 段精选音频',
    proofRating: '健康健美 · App Store',
    stat1n: '26', stat1: '精选音频',
    stat2n: 'iOS · iPadOS', stat2: '原生体验',
    stat3n: 'EN / 中文', stat3: '双语界面',
    stat4n: '0', stat4: '广告 · 追踪',
    howTitle: '三步进入平静', howSub: '选一个当下，跟着呼吸，剩下的交给引导。',
    step1Title: '选择你的当下', step1Desc: '入睡、缓解焦虑、专注，或自定义时长的计时冥想——挑一个开始。',
    step2Title: '跟着呼吸与引导', step2Desc: '专业语音与精选音频陪你一步步放松，AI 教练随时解答与调整。',
    step3Title: '记录心情与坚持', step3Desc: '每次练习自动记录时长与心情，看着你的正念旅程一天天累积。',
    featuresTitle: '功能特色', featuresSub: '为放松、入睡与专注设计的每一个细节。',
    feature1Title: 'AI 智能对话', feature1Desc: '个性化冥想指导，实时解答疑问，并根据你的历史记录给出建议。',
    feature2Title: '音乐冥想', feature2Desc: '26 段精选冥想音频，涵盖自然环境音与专业冥想音乐。',
    feature3Title: '引导冥想', feature3Desc: '专业语音引导，含 7 天深度睡眠计划与缓解焦虑系列课程。',
    feature4Title: '计时冥想', feature4Desc: '自定义冥想时长，多种背景音乐可选，专注于呼吸练习。',
    feature5Title: '冥想记录', feature5Desc: '详细的冥想历史与心情追踪，进度统计一目了然。',
    feature6Title: '隐私优先', feature6Desc: '支持中英双语，无需注册即可使用，你的数据保存在本地。',
    screenshotsTitle: '应用截图', screenshotsSub: '左右滑动，点击可放大查看。',
    shot1: '简洁优雅的主界面', shot2: '精美的播放控制',
    privacyTitle: '你的练习，只属于你', privacyDesc: '无需注册，无需账号。冥想历史与心情记录保存在设备本地，我们不追踪、不贩卖你的数据。',
    pp1: '数据本地存储', pp2: '无需注册', pp3: '无任何追踪', pp4: '无广告干扰',
    faqTitle: '常见问题',
    q1: '需要注册或联网吗？', a1: '无需注册即可使用。计时冥想与已缓存的音频可离线练习；AI 对话需要联网。',
    q2: '一共有多少冥想音频？', a2: '内置 26 段精选冥想音频，涵盖自然环境音与专业冥想音乐，持续更新。',
    q3: '适合冥想初学者吗？', a3: '非常适合。引导冥想、7 天深度睡眠计划与缓解焦虑系列会一步步带你入门。',
    q4: '我的隐私如何保护？', a4: '冥想历史与心情记录都保存在设备本地，无需账号，我们不追踪、不贩卖任何个人数据。',
    q5: '支持哪些设备和语言？', a5: '支持 iPhone 与 iPad（iOS / iPadOS），提供简体中文与英文双语界面。',
    q6: '这款 App 收费吗？', a6: 'App 可免费下载使用，开启你的正念之旅无需任何门槛。',
    downloadTitle: '开启你的正念之旅', downloadSubtitle: '立即下载 AI冥想，从今晚睡得更深开始。', downloadNote: '支持 iPhone 与 iPad · 免费下载',
    footerTagline: '你的智能冥想伙伴，陪你呼吸、入睡与专注。',
    footerQuick: '快速链接', footerPrivacy: '隐私政策', footerSupport: '技术支持', footerContact: '联系我们',
    footerEmail: '邮箱', footerWebsite: '网站', footerRights: 'WeiProduct. 保留所有权利。',
    stickySub: 'AI 冥想教练 · 免费', stickyGet: '获取',
    eyeHow: '使用方式', eyeTry: '亲自体验', eyeMoments: '为这些时刻而生', eyeFeatures: '核心功能',
    eyeLibrary: '内容库', eyeShots: '真实截图', eyePrivacy: '隐私承诺', eyeCompare: '对比', eyeFaq: '答疑',
    tryTitle: '在这里，先试一分钟', trySub: '不用下载也能先呼吸一次——选一个节奏开始。',
    tryP1: '入睡 4-7-8', tryP2: '专注方块 4-4-4-4', tryP3: '平静 4-6',
    tryStart: '开始 1 分钟', tryStop: '停止', tryReady: '准备好了就开始',
    tryInhale: '吸气', tryHold: '屏息', tryExhale: '呼气', tryLeft: '还剩 {s} 秒',
    tryDoneTitle: '感觉到了吗？', tryDoneDesc: 'App 里还有 26 段引导等你。', tryDoneBtn: 'App Store 下载',
    momentsTitle: '总有一个时刻，适合安静下来', momentsSub: '四个真实场景，四种进入方式。',
    m1Title: '睡不着的夜', m1Desc: '思绪停不下来？让引导语音替你放慢呼吸，把身体先送入睡眠。', m1Tag: '7 天深睡计划',
    m2Title: '高压的下午', m2Desc: '会议之间的十分钟，做一段快速平复练习，有疑问随时问 AI 教练。', m2Tag: '缓解焦虑 + AI 教练',
    m3Title: '想养成习惯的早晨', m3Desc: '出门前的五分钟计时冥想，心情与连续天数自动记下，习惯悄悄长成。', m3Tag: '计时冥想 + 记录',
    m4Title: '第一次尝试冥想', m4Desc: '不知道从哪开始？引导冥想一步一步带你入门，零基础也不慌。', m4Tag: '引导冥想',
    libTitle: '看看里面都有什么', libSub: '从 7 晚的睡眠旅程到自由计时，每一段都有它的位置。',
    libPlanTitle: '7 天深度睡眠计划', libPlanDesc: '每晚一段引导，循序渐进，从放松身体到自然入睡。',
    libN1: '第 1 晚', libN2: '第 2 晚', libN3: '第 3 晚', libN4: '第 4 晚', libN5: '第 5 晚', libN6: '第 6 晚', libN7: '第 7 晚',
    libD1: '放松身体', libD2: '放慢呼吸', libD3: '身体扫描', libD4: '放下思绪', libD5: '延长呼气', libD6: '安定心绪', libD7: '自然入睡',
    libC1t: '缓解焦虑系列', libC1d: '高压时刻的快速平复练习。',
    libC2t: '自然环境音', libC2d: '雨声、森林与海浪，如临其境。',
    libC3t: '专业冥想音乐', libC3d: '为放松与专注而作的音乐。',
    libC4t: '自由计时', libC4d: '任意时长，背景音由你选。',
    libChip: '26 段精选，持续更新',
    shot4: '背景音随心选',
    compareTitle: '为什么是 AI冥想', compareSub: '同样是想安静下来，体验可以很不一样。',
    cCol1: '自己摸索', cCol2: '传统冥想 App', cCol3: 'AI冥想',
    cR1: '个性化指导', cR1a: '没有反馈，全靠自己琢磨', cR1b: '固定课程，人人相同', cR1c: 'AI 教练根据你的记录调整',
    cR2: '上手门槛', cR2a: '不知道从哪开始', cR2b: '先注册，再挑方案', cR2c: '无需注册，30 秒开始',
    cR3: '你的数据', cR3b: '常常同步到云端', cR3c: '全部留在设备上',
    cR4: '价格', cR4a: '免费，但没有引导', cR4b: '通常需要订阅', cR4c: '免费下载',
    cR5: '语言', cR5b: '多为单一语言', cR5c: '中英双语',
    footerGet: '获取 App', footerScan: '扫码前往 App Store',
    themeDark: '深色模式', themeLight: '浅色模式',
    /* AI-AVATAR */
    aiTitle: 'AI分身 · AI冥想助手',
    aiGreeting: '你好！我是 AI冥想 的 AI分身 🧘 关于引导冥想、7 天深睡计划、AI 教练、隐私或价格，都可以问我。',
    aiPlaceholder: '输入你的问题…',
    aiSend: '发送',
    aiChip1: 'AI 冥想教练能帮我做什么？',
    aiChip2: '适合冥想初学者吗？',
    aiChip3: '需要注册或付费吗？',
    aiDisclaimer: 'AI 生成，仅供参考',
    aiError: '抱歉，AI 助手暂时连不上，请稍后再试。(Sorry, the assistant is temporarily unreachable — please try again later.)'
    /* /AI-AVATAR */
  },
  'en': {
    skip: 'Skip to content',
    brandName: 'AI Meditation',
    navHow: 'How it works', navFeatures: 'Features', navScreenshots: 'Screenshots', navFaq: 'FAQ', navGet: 'Get App',
    heroEyebrow: '✦ Your AI meditation coach',
    heroTitle: 'Sleep deeper. Worry less. Focus sharper.',
    heroSubtitle: 'An AI coach guides you to breathe, fall asleep, and calm anxiety — 26 curated sessions, no sign-up ever.',
    downloadBtn: 'Download on App Store', learnMore: 'Learn more',
    trust1: 'No sign-up', trust2: 'Stored on-device', trust3: 'EN / 中文',
    breatheIn: 'Breathe in', breatheOut: 'Breathe out',
    chipA: '7-day sleep plan', chipB: '26 curated tracks',
    proofRating: 'Health & Fitness · App Store',
    stat1n: '26', stat1: 'Curated sessions',
    stat2n: 'iOS · iPadOS', stat2: 'Native feel',
    stat3n: 'EN / 中文', stat3: 'Bilingual',
    stat4n: '0', stat4: 'Ads · Tracking',
    howTitle: 'Three steps to calm', howSub: 'Pick a moment, follow your breath — the guide does the rest.',
    step1Title: 'Pick your moment', step1Desc: 'Sleep, anxiety relief, focus, or a timed session of any length — choose one and begin.',
    step2Title: 'Breathe & follow along', step2Desc: 'Professional voice and curated audio ease you down step by step, with the AI coach on call.',
    step3Title: 'Track mood & streak', step3Desc: 'Every session logs its length and your mood, so your mindfulness journey builds day by day.',
    featuresTitle: 'Features', featuresSub: 'Every detail crafted for rest, sleep, and focus.',
    feature1Title: 'AI Smart Chat', feature1Desc: 'Personalized meditation guidance, real-time answers, and suggestions based on your history.',
    feature2Title: 'Music Meditation', feature2Desc: '26 curated meditation tracks spanning natural soundscapes and professional meditation music.',
    feature3Title: 'Guided Meditation', feature3Desc: 'Professional voice guidance with a 7-day deep-sleep program and an anxiety-relief series.',
    feature4Title: 'Timer Meditation', feature4Desc: 'Set any duration, pick from multiple background tracks, and focus purely on your breath.',
    feature5Title: 'Meditation Tracking', feature5Desc: 'Detailed history with mood tracking and clear progress statistics at a glance.',
    feature6Title: 'Privacy First', feature6Desc: 'Bilingual and ready to use with no sign-up — your data stays on your device.',
    screenshotsTitle: 'Screenshots', screenshotsSub: 'Swipe through — tap any shot to zoom in.',
    shot1: 'A clean, elegant home', shot2: 'Beautiful playback controls',
    privacyTitle: 'Your practice is yours alone', privacyDesc: 'No sign-up, no account. Your meditation history and moods stay on your device — we never track or sell your data.',
    pp1: 'Stored on-device', pp2: 'No sign-up', pp3: 'Zero tracking', pp4: 'No ads',
    faqTitle: 'Frequently Asked Questions',
    q1: 'Do I need an account or internet?', a1: 'No sign-up needed. Timer sessions and cached audio work offline; the AI chat requires an internet connection.',
    q2: 'How many meditation tracks are there?', a2: '26 curated meditation tracks are built in, spanning natural soundscapes and professional meditation music, with more added over time.',
    q3: 'Is it good for beginners?', a3: 'Absolutely. Guided meditations, a 7-day deep-sleep program, and an anxiety-relief series ease you in step by step.',
    q4: 'How is my privacy protected?', a4: 'Your meditation history and moods are stored on-device with no account required — we never track or sell any personal data.',
    q5: 'Which devices and languages are supported?', a5: 'iPhone and iPad (iOS / iPadOS), with full Simplified Chinese and English interfaces.',
    q6: 'Is the app free?', a6: 'The app is free to download and use, so there is nothing standing between you and your mindfulness journey.',
    downloadTitle: 'Start your mindfulness journey', downloadSubtitle: 'Download AI Meditation and begin sleeping deeper tonight.', downloadNote: 'iPhone & iPad · Free to download',
    footerTagline: 'Your smart meditation companion for breathing, sleep, and focus.',
    footerQuick: 'Quick Links', footerPrivacy: 'Privacy Policy', footerSupport: 'Support', footerContact: 'Contact',
    footerEmail: 'Email', footerWebsite: 'Website', footerRights: 'WeiProduct. All rights reserved.',
    stickySub: 'AI meditation coach · Free', stickyGet: 'Get',
    eyeHow: 'HOW IT WORKS', eyeTry: 'TRY IT', eyeMoments: 'MADE FOR THESE MOMENTS', eyeFeatures: 'FEATURES',
    eyeLibrary: 'THE LIBRARY', eyeShots: 'SCREENSHOTS', eyePrivacy: 'PRIVACY', eyeCompare: 'WHY US', eyeFaq: 'FAQ',
    tryTitle: 'Try one minute, right here', trySub: 'No download needed for your first breath — pick a rhythm and begin.',
    tryP1: 'Sleep 4-7-8', tryP2: 'Focus box 4-4-4-4', tryP3: 'Calm 4-6',
    tryStart: 'Start 1 minute', tryStop: 'Stop', tryReady: 'Ready when you are',
    tryInhale: 'Inhale', tryHold: 'Hold', tryExhale: 'Exhale', tryLeft: '{s}s left',
    tryDoneTitle: 'Felt that?', tryDoneDesc: '26 guided sessions await in the app.', tryDoneBtn: 'Download on App Store',
    momentsTitle: 'There is always a moment made for stillness', momentsSub: 'Four real moments, four ways in.',
    m1Title: 'The racing-mind night', m1Desc: 'Thoughts won’t stop? Let a guided voice slow your breath and ease your body into sleep first.', m1Tag: '7-day sleep plan',
    m2Title: 'The pressure-cooker afternoon', m2Desc: 'Ten minutes between meetings: a quick calming session, with the AI coach on call for anything.', m2Tag: 'Anxiety relief + AI coach',
    m3Title: 'The habit-building morning', m3Desc: 'Five timed minutes before you head out — mood and streak logged automatically, a habit quietly grows.', m3Tag: 'Timer + streak log',
    m4Title: 'The very first session', m4Desc: 'Not sure where to start? Guided meditations walk you in step by step — no experience needed.', m4Tag: 'Guided meditations',
    libTitle: 'A look inside the library', libSub: 'From a 7-night sleep journey to an open timer, every session has its place.',
    libPlanTitle: '7-day deep-sleep plan', libPlanDesc: 'One guided session each night, easing you from unwinding to falling asleep naturally.',
    libN1: 'Night 1', libN2: 'Night 2', libN3: 'Night 3', libN4: 'Night 4', libN5: 'Night 5', libN6: 'Night 6', libN7: 'Night 7',
    libD1: 'Unwind the body', libD2: 'Slow the breath', libD3: 'Body scan', libD4: 'Let thoughts drift', libD5: 'Longer exhales', libD6: 'Settle the mind', libD7: 'Drift off naturally',
    libC1t: 'Anxiety-relief series', libC1d: 'Quick calm-downs for high-pressure moments.',
    libC2t: 'Natural soundscapes', libC2d: 'Rain, forest, and ocean waves.',
    libC3t: 'Meditation music', libC3d: 'Composed for rest and focus.',
    libC4t: 'Open timer', libC4d: 'Any length, your choice of backdrop.',
    libChip: '26 curated tracks, growing',
    shot4: 'Pick your backdrop sound',
    compareTitle: 'Why AI Meditation', compareSub: 'Same goal — a quieter mind. Very different experiences.',
    cCol1: 'Going it alone', cCol2: 'A typical meditation app', cCol3: 'AI Meditation',
    cR1: 'Personalized guidance', cR1a: 'No feedback, all guesswork', cR1b: 'Fixed courses, same for everyone', cR1c: 'An AI coach adapts to your history',
    cR2: 'Getting started', cR2a: 'Hard to know where to begin', cR2b: 'Sign up first, then pick a plan', cR2c: 'No sign-up, breathing in 30 seconds',
    cR3: 'Your data', cR3b: 'Often synced to the cloud', cR3c: 'Stays on your device',
    cR4: 'Price', cR4a: 'Free, but unguided', cR4b: 'Usually a subscription', cR4c: 'Free to download',
    cR5: 'Language', cR5b: 'Mostly single-language', cR5c: 'EN + 中文',
    footerGet: 'Get the app', footerScan: 'Scan for the App Store',
    themeDark: 'Dark mode', themeLight: 'Light mode',
    /* AI-AVATAR */
    aiTitle: 'AI Avatar · AI Meditation Assistant',
    aiGreeting: 'Hi! I\'m the AI avatar for AI Meditation 🧘 Ask me about guided sessions, the 7-day sleep plan, the AI coach, privacy, or pricing.',
    aiPlaceholder: 'Type your question…',
    aiSend: 'Send',
    aiChip1: 'What can the AI meditation coach do?',
    aiChip2: 'Is it good for beginners?',
    aiChip3: 'Do I need an account? Is it free?',
    aiDisclaimer: 'AI-generated · for reference only',
    aiError: 'Sorry, the assistant is temporarily unreachable — please try again later. （抱歉，AI 助手暂时连不上，请稍后再试。）'
    /* /AI-AVATAR */
  }
};

let currentLang = 'zh-CN';

function applyLang(lang) {
  currentLang = I18N[lang] ? lang : 'zh-CN';
  const t = I18N[currentLang];
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const k = el.getAttribute('data-i18n');
    if (t[k] !== undefined) el.textContent = t[k];
  });
  /* AI-AVATAR: translate placeholder attributes */
  document.querySelectorAll('[data-i18n-ph]').forEach(el => {
    const k = el.getAttribute('data-i18n-ph');
    if (t[k] !== undefined) el.setAttribute('placeholder', t[k]);
  });
  /* /AI-AVATAR */
  document.documentElement.lang = currentLang;
  document.title = currentLang === 'zh-CN'
    ? 'AI冥想 - 智能冥想助手 | AI Meditation'
    : 'AI Meditation - Sleep, Calm & Focus Coach';
  document.querySelectorAll('.js-lang-switch').forEach(b => {
    b.textContent = currentLang === 'zh-CN' ? 'EN' : '中文';
  });
  // Swap privacy / support pages to the matching language
  const priv = document.querySelector('a[data-page="privacy"]');
  if (priv) priv.href = currentLang === 'zh-CN' ? 'privacy.html' : 'privacy-en.html';
  const sup = document.querySelector('a[data-page="support"]');
  if (sup) sup.href = currentLang === 'zh-CN' ? 'support.html' : 'support-en.html';
  try { localStorage.setItem('lang', currentLang); } catch (e) {}
}

function initLang() {
  let saved;
  try { saved = localStorage.getItem('lang'); } catch (e) {}
  if (!saved) saved = (navigator.language || '').toLowerCase().startsWith('zh') ? 'zh-CN' : 'en';
  applyLang(saved);
}

// ===== Theme =====
function setTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  const meta = document.getElementById('themeColorMeta');
  if (meta) meta.setAttribute('content', theme === 'dark' ? '#0e1020' : '#1e2846');
  document.querySelectorAll('.js-theme-label').forEach(sp => {
    const key = theme === 'dark' ? 'themeLight' : 'themeDark';
    sp.setAttribute('data-i18n', key);
    sp.textContent = I18N[currentLang][key];
  });
  try { localStorage.setItem('theme', theme); } catch (e) {}
}
function initTheme() {
  let saved;
  try { saved = localStorage.getItem('theme'); } catch (e) {}
  if (!saved) saved = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
  setTheme(saved);
}

// ===== Gallery =====
function initGallery() {
  const track = document.getElementById('galTrack');
  if (!track) return;
  const shots = Array.from(track.children);
  const dotsWrap = document.getElementById('galDots');
  const prev = document.getElementById('galPrev');
  const next = document.getElementById('galNext');

  shots.forEach((_, i) => {
    const b = document.createElement('button');
    b.type = 'button';
    b.setAttribute('aria-label', 'screenshot ' + (i + 1));
    if (i === 0) b.classList.add('active');
    b.addEventListener('click', () => shots[i].scrollIntoView({ behavior: 'smooth', inline: 'center', block: 'nearest' }));
    dotsWrap.appendChild(b);
  });
  const dots = Array.from(dotsWrap.children);

  function activeIndex() {
    const c = track.scrollLeft + track.clientWidth / 2;
    let best = 0, bd = Infinity;
    shots.forEach((s, i) => {
      const center = s.offsetLeft + s.offsetWidth / 2;
      const d = Math.abs(center - c);
      if (d < bd) { bd = d; best = i; }
    });
    return best;
  }
  track.addEventListener('scroll', () => {
    const i = activeIndex();
    dots.forEach((d, j) => d.classList.toggle('active', j === i));
  }, { passive: true });

  const step = () => (shots[1] ? shots[1].offsetLeft - shots[0].offsetLeft : 300);
  if (prev) prev.addEventListener('click', () => track.scrollBy({ left: -step(), behavior: 'smooth' }));
  if (next) next.addEventListener('click', () => track.scrollBy({ left: step(), behavior: 'smooth' }));

  // Lightbox
  const lb = document.createElement('div');
  lb.className = 'lightbox';
  lb.innerHTML = '<button class="lightbox-close" aria-label="close">&times;</button><img alt="">';
  document.body.appendChild(lb);
  const lbImg = lb.querySelector('img');
  const close = () => lb.classList.remove('open');
  lb.addEventListener('click', e => { if (e.target === lb || e.target.classList.contains('lightbox-close')) close(); });
  document.addEventListener('keydown', e => { if (e.key === 'Escape') close(); });
  track.querySelectorAll('.phone').forEach(p => {
    p.addEventListener('click', () => {
      const img = p.querySelector('img');
      lbImg.src = img.src; lbImg.alt = img.alt;
      lb.classList.add('open');
    });
  });
}

// ===== Scroll reveal =====
function initReveal() {
  const els = document.querySelectorAll('.reveal');
  if (!('IntersectionObserver' in window)) { els.forEach(e => e.classList.add('in')); return; }
  const io = new IntersectionObserver((entries) => {
    entries.forEach(en => {
      if (en.isIntersecting) { en.target.classList.add('in'); io.unobserve(en.target); }
    });
  }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
  els.forEach((el, i) => { el.style.transitionDelay = (Math.min(i, 6) * 0.05) + 's'; io.observe(el); });
}

// ===== Nav + sticky + progress =====
function initScroll() {
  const nav = document.getElementById('navbar');
  const sticky = document.getElementById('stickyCta');
  const prog = document.getElementById('scrollProgress');
  const onScroll = () => {
    const y = window.scrollY;
    if (nav) nav.classList.toggle('scrolled', y > 20);
    if (sticky) sticky.classList.toggle('show', y > 620);
    if (prog) {
      const max = document.documentElement.scrollHeight - window.innerHeight;
      prog.style.width = (max > 0 ? Math.min(100, (y / max) * 100) : 0) + '%';
    }
  };
  window.addEventListener('scroll', onScroll, { passive: true });
  window.addEventListener('resize', onScroll, { passive: true });
  onScroll();
}

// ===== Try-a-minute breathing widget =====
function initBreath() {
  const btn = document.getElementById('tryStartBtn');
  const orb = document.getElementById('tryOrb');
  const phaseEl = document.getElementById('tryPhase');
  const countEl = document.getElementById('tryCount');
  const prog = document.getElementById('tryProgress');
  const doneCard = document.getElementById('tryDone');
  if (!btn || !orb || !phaseEl || !countEl || !prog) return;
  const btnLabel = btn.querySelector('span');
  const chips = Array.from(document.querySelectorAll('.try-chip'));
  const reduce = window.matchMedia('(prefers-reduced-motion: reduce)');
  const CIRC = 2 * Math.PI * 96;
  const TOTAL = 60;
  prog.style.strokeDasharray = CIRC;
  prog.style.strokeDashoffset = CIRC;

  let pattern = [4, 7, 8, 0];
  let running = false, raf = 0, t0 = 0;
  let lastPhase = '', lastCount = '';

  const easeInOut = t => (t < 0.5 ? 2 * t * t : 1 - Math.pow(-2 * t + 2, 2) / 2);

  function phaseAt(elapsed) {
    const segs = [
      { key: 'tryInhale', dur: pattern[0], from: 0.72, to: 1.14 },
      { key: 'tryHold', dur: pattern[1], from: 1.14, to: 1.14 },
      { key: 'tryExhale', dur: pattern[2], from: 1.14, to: 0.72 },
      { key: 'tryHold', dur: pattern[3], from: 0.72, to: 0.72 }
    ].filter(s => s.dur > 0);
    const cycle = segs.reduce((a, s) => a + s.dur, 0);
    let t = elapsed % cycle;
    for (const s of segs) {
      if (t < s.dur) return { key: s.key, scale: s.from + (s.to - s.from) * easeInOut(t / s.dur) };
      t -= s.dur;
    }
    return { key: segs[0].key, scale: segs[0].from };
  }

  function tick(now) {
    const elapsed = (now - t0) / 1000;
    if (elapsed >= TOTAL) { stop(true); return; }
    const t = I18N[currentLang];
    const p = phaseAt(elapsed);
    if (!reduce.matches) orb.style.transform = 'translate(-50%, -50%) scale(' + p.scale.toFixed(3) + ')';
    const pTxt = t[p.key];
    if (pTxt !== lastPhase) { phaseEl.textContent = pTxt; lastPhase = pTxt; }
    const cTxt = t.tryLeft.replace('{s}', Math.ceil(TOTAL - elapsed));
    if (cTxt !== lastCount) { countEl.textContent = cTxt; lastCount = cTxt; }
    prog.style.strokeDashoffset = CIRC * (1 - elapsed / TOTAL);
    raf = requestAnimationFrame(tick);
  }

  function start() {
    running = true;
    if (doneCard) doneCard.hidden = true;
    t0 = performance.now();
    lastPhase = ''; lastCount = '';
    phaseEl.removeAttribute('data-i18n');
    btnLabel.setAttribute('data-i18n', 'tryStop');
    btnLabel.textContent = I18N[currentLang].tryStop;
    prog.style.strokeDashoffset = CIRC;
    raf = requestAnimationFrame(tick);
  }

  function stop(finished) {
    running = false;
    cancelAnimationFrame(raf);
    btnLabel.setAttribute('data-i18n', 'tryStart');
    btnLabel.textContent = I18N[currentLang].tryStart;
    phaseEl.setAttribute('data-i18n', 'tryReady');
    phaseEl.textContent = I18N[currentLang].tryReady;
    countEl.textContent = '';
    orb.style.transform = '';
    prog.style.strokeDashoffset = finished ? 0 : CIRC;
    if (finished && doneCard) doneCard.hidden = false;
  }

  btn.addEventListener('click', () => (running ? stop(false) : start()));

  chips.forEach(ch => ch.addEventListener('click', () => {
    chips.forEach(c => c.classList.toggle('active', c === ch));
    pattern = ch.getAttribute('data-pattern').split(',').map(Number);
    if (running) { t0 = performance.now(); prog.style.strokeDashoffset = CIRC; }
  }));
}

// ===== Init =====
document.addEventListener('DOMContentLoaded', () => {
  initTheme();
  initLang();
  initGallery();
  initReveal();
  initScroll();
  initBreath();

  const yr = document.getElementById('currentYear');
  if (yr) yr.textContent = new Date().getFullYear();

  document.querySelectorAll('.js-lang-switch').forEach(b => {
    b.addEventListener('click', () => applyLang(currentLang === 'zh-CN' ? 'en' : 'zh-CN'));
  });
  document.querySelectorAll('.js-theme-toggle').forEach(b => {
    b.addEventListener('click', () => setTheme(document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark'));
  });

  document.querySelectorAll('a[href^="#"]').forEach(a => {
    a.addEventListener('click', function (e) {
      const id = this.getAttribute('href');
      if (id.length > 1) {
        const target = document.querySelector(id);
        if (target) { e.preventDefault(); target.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
      }
    });
  });
});

/* AI-AVATAR: floating "AI分身" assistant widget */
(function () {
  const AI_PROXY_URL = 'https://personal-portfolio-api-sandy.vercel.app/api/chat-proxy';
  const AI_SYSTEM_PROMPT = [
    'You are the "AI分身" (AI avatar) assistant on the promo website of AI Meditation (AI冥想), an iOS meditation app by WeiProduct.',
    '',
    'App facts (the ONLY facts you may state):',
    '- One-liner: sleep deeper, worry less, focus sharper — an AI meditation coach guides you to breathe, fall asleep, and calm anxiety.',
    '- Key features: AI meditation coach chat (personalized guidance, real-time answers, suggestions based on your practice history); 26 curated audio tracks spanning natural soundscapes (rain, forest, ocean waves) and professional meditation music; guided meditations including a 7-day deep-sleep program and an anxiety-relief series; timer meditation with custom duration and a choice of background sounds; meditation history with mood tracking, streaks, and progress statistics.',
    '- Privacy: no sign-up or account required; meditation history and mood records are stored on-device; no tracking, no ads, no selling data. Timer sessions and cached audio work offline; only the AI chat needs an internet connection.',
    '- Platform: iPhone and iPad (iOS / iPadOS).',
    '- Price: free.',
    '- Languages: English and Simplified Chinese (中文).',
    '- App Store link: https://apps.apple.com/app/id6749164175',
    '',
    'Style rules:',
    '- ALWAYS reply in the same language as the user\'s most recent message: English question → English answer, 中文提问 → 中文回答. Do NOT default to Chinese just because the app has a Chinese name.',
    '- Keep replies to 1-3 short sentences; be friendly and concrete.',
    '- NEVER invent download counts, ratings, reviews, or features not listed above.',
    '- If asked about unrelated topics, politely steer the conversation back to AI Meditation.',
    '- When the user wants to download or try the app, point them to the App Store link.'
  ].join('\n');
  const AI_MAX_HISTORY = 12;

  document.addEventListener('DOMContentLoaded', () => {
    const toggle = document.getElementById('aiToggle');
    const panel = document.getElementById('aiPanel');
    const closeBtn = document.getElementById('aiClose');
    const msgs = document.getElementById('aiMsgs');
    const chipsWrap = document.getElementById('aiChips');
    const form = document.getElementById('aiForm');
    const input = document.getElementById('aiInput');
    const sendBtn = document.getElementById('aiSendBtn');
    if (!toggle || !panel || !msgs || !form || !input) return;

    let history = [];
    let greeted = false;
    let busy = false;

    function addBubble(role, text, i18nKey) {
      const div = document.createElement('div');
      div.className = 'ai-msg ' + (role === 'user' ? 'user' : 'bot');
      if (i18nKey) {
        div.setAttribute('data-i18n', i18nKey); // follows future language switches too
        div.textContent = I18N[currentLang][i18nKey];
      } else {
        div.textContent = text;
      }
      msgs.appendChild(div);
      msgs.scrollTop = msgs.scrollHeight;
      return div;
    }

    function showTyping() {
      const div = document.createElement('div');
      div.className = 'ai-msg bot ai-typing';
      div.innerHTML = '<span></span><span></span><span></span>';
      msgs.appendChild(div);
      msgs.scrollTop = msgs.scrollHeight;
      return div;
    }

    function openPanel() {
      panel.hidden = false;
      toggle.setAttribute('aria-expanded', 'true');
      if (!greeted) { greeted = true; addBubble('bot', '', 'aiGreeting'); }
      input.focus();
    }
    function closePanel() {
      panel.hidden = true;
      toggle.setAttribute('aria-expanded', 'false');
      toggle.focus();
    }

    async function send(text) {
      text = (text || '').trim();
      if (!text || busy) return;
      busy = true;
      if (sendBtn) sendBtn.disabled = true;
      if (chipsWrap) chipsWrap.hidden = true;
      addBubble('user', text);
      history.push({ role: 'user', content: text });
      history = history.slice(-AI_MAX_HISTORY);
      const typing = showTyping();
      try {
        const res = await fetch(AI_PROXY_URL, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            model: 'gpt-4o-mini',
            messages: [{ role: 'system', content: AI_SYSTEM_PROMPT }].concat(history),
            max_tokens: 350
          })
        });
        if (!res.ok) throw new Error('HTTP ' + res.status);
        const data = await res.json();
        let reply = '';
        if (data && data.choices && data.choices[0]) {
          const m = data.choices[0].message;
          reply = (m && m.content) || data.choices[0].text || '';
        }
        if (!reply && data && typeof data.content === 'string') reply = data.content;
        if (!reply && data && typeof data.reply === 'string') reply = data.reply;
        if (!reply && data && typeof data.message === 'string') reply = data.message;
        reply = (reply || '').trim();
        if (!reply) throw new Error('empty reply');
        typing.remove();
        addBubble('bot', reply);
        history.push({ role: 'assistant', content: reply });
        history = history.slice(-AI_MAX_HISTORY);
      } catch (err) {
        typing.remove();
        addBubble('bot', '', 'aiError');
      } finally {
        busy = false;
        if (sendBtn) sendBtn.disabled = false;
      }
    }

    toggle.addEventListener('click', () => (panel.hidden ? openPanel() : closePanel()));
    if (closeBtn) closeBtn.addEventListener('click', closePanel);
    document.addEventListener('keydown', e => { if (e.key === 'Escape' && !panel.hidden) closePanel(); });
    if (chipsWrap) chipsWrap.querySelectorAll('.ai-chip').forEach(chip => {
      chip.addEventListener('click', () => send(chip.textContent));
    });
    form.addEventListener('submit', e => {
      e.preventDefault();
      const v = input.value;
      input.value = '';
      send(v);
    });

    // Dev/verify affordance: ?aichat=open auto-opens; ?aichat=demo also sends chip 1 for real.
    const q = location.search;
    if (q.indexOf('aichat=open') !== -1 || q.indexOf('aichat=demo') !== -1) {
      openPanel();
      if (q.indexOf('aichat=demo') !== -1) {
        setTimeout(() => send(I18N[currentLang].aiChip1), 600);
      }
    }
  });
})();
/* /AI-AVATAR */
